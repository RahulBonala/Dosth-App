import ProfileContent from './ProfileContent';

export const metadata = {
  title: 'Profile',
  description:
    'Manage your Dosth profile, view repair history, donation stats, and account settings.',
};

export default function ProfilePage() {
  return <ProfileContent />;
}
