import LoginContent from './LoginContent';

export const metadata = {
  title: 'Login',
  description:
    'Log in to your Dosth account with phone OTP or email to access repair services and donations.',
};

export default function LoginPage() {
  return <LoginContent />;
}
