import SignupContent from './SignupContent';

export const metadata = {
  title: 'Sign Up',
  description:
    'Create your Dosth account and join 50,000+ users accessing trusted repair services and community support.',
};

export default function SignupPage() {
  return <SignupContent />;
}
